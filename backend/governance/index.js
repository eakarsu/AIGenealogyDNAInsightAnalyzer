'use strict';const{createGovernedRouter}=require('./router');const pool=require('../db');const{authenticateToken:auth}=require('../middleware/auth');const{evaluate}=require('./domain');module.exports=createGovernedRouter({pool,auth,evaluate,workflowType:'genetic-genealogy',providers:['approved-genetic-provider','family-tree','archive','identity','encrypted-storage'],approverRoles:['data_owner','privacy_officer','genealogist','admin']});
